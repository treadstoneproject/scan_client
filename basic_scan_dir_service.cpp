#include "internet/scan_client/basic_scan_dir_service.hpp"

namespace internet
{

    namespace service
    {


        //Move to write in basic_scan_dir_service.cpp. boost::asio::io_service::service not support
        /*
        template<typename ScanDirImplementation>
        void basic_scan_dir_service<ScanDirImplementation>::construct(implementation_type& impl)
        {

        impl->read_msg_scan();
        }
        */

        /*
        template<typename ScanDirImplementation>
        void basic_scan_dir_service<ScanDirImplementation>::destroy(implementation_type&  impl)
        {
        impl->destroy();
        impl->reset();
        }
        */

        /*
        template<typename ScanDirImplementation>
        void basic_scan_dir_service<ScanDirImplementation>::shutdown_service()
        {

        }
        */
				/*
        template<typename ScanDirImplementation>
        void basic_scan_dir_service<ScanDirImplementation>::add_directory(implementation_type& impl,
                const std::string& dir_name)
        {
            impl->add_directory(dir_name);
        }
			 */
    }
}
